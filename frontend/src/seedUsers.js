/**
 * 예시 계정이 "무엇을 확인하기 위한 것인지" 적어둔 설명표.
 * 사용자 목록 자체는 서버(GET /api/users)에서 받고, 여기 있는 것은 곁들일 설명뿐이다.
 * 이 설명은 사용자의 속성이 아니라 예시 데이터를 만든 의도라 DB에 두지 않았다.
 *
 * 키는 백엔드 DataInitializer가 넣는 예시 계정의 id다. 그쪽 구성을 바꾸면 여기도 고쳐야 한다.
 */
export const SEED_USER_HINTS = {
	1: '프로젝트1 소유자 — 수정·삭제·멤버 관리·OWNER 임명까지 전부 가능',
	2: '프로젝트1 관리자 — 수정·멤버 관리는 되지만 삭제와 OWNER 임명은 막힘',
	3: '프로젝트1 멤버 — 작업 생성은 되고, 수정은 자기 담당 작업만',
	4: '프로젝트2 멤버 — 프로젝트1은 목록에 아예 보이지 않음(데이터 격리)',
	5: '어느 프로젝트에도 속하지 않음 — 비멤버는 조회조차 못 함',
	6: '프로젝트1은 멤버, 프로젝트2는 소유자 — 프로젝트마다 역할이 다른 사례',
}

export const CREATED_USER_HINT = '사용자가 등록한 계정'

/** 사용자를 고르는 드롭다운이 두 곳이라, 문구가 갈리지 않도록 판단을 여기 모은다. */
export const userHint = (id) => SEED_USER_HINTS[id] ?? CREATED_USER_HINT
