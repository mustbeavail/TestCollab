plugins {
	// toolchain에 지정한 JDK가 로컬에 없으면 자동으로 내려받아 사용한다.
	// 평가자 PC에 JDK 17이 없어도 ./gradlew 한 번으로 동작하게 하기 위함.
	id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'collab'
